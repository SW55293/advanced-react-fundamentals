import React from 'react'

function ProfileSettings() {
    return (
        <section>
            <h1>Your settings</h1>
            <input type="password"/>
            <button>Change</button>
        </section>
    )
}

export default ProfileSettings